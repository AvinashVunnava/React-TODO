import { makeStyles } from '@material-ui/core/styles'

//TODO: Need to use theme from MUI
export default makeStyles((theme) => ({
  container: {
    marginTop: '48px',
  },
  inputContainer: {
    width: '30em',
  },
  btnContainer: {
    marginTop: '4px',
    marginLeft: '26em',
  },
}))
